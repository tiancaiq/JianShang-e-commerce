import { CurrencyPipe, DatePipe } from '@angular/common';
import { Component, OnInit, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { AdminPaymentDetail, AdminRefundApproval, AdminRefundExecution, AdminRefundPreview, AdminRefundRequest } from '../../core/models/admin-finance.model';
import { AdminFinanceService } from '../../core/services/admin-finance.service';

@Component({selector:'app-admin-payment-detail',standalone:true,imports:[FormsModule,RouterLink,CurrencyPipe,DatePipe],template:`
<section class="page"><a routerLink="/admin/payments">← Payments</a>@if(error()){<p class="alert" role="alert">{{error()}}</p>}@if(!detail()){<p class="card">Loading payment record…</p>}@else if(detail();as d){<header><div><p>Payment {{d.payment.paymentId}}</p><h1>{{d.payment.capturedAmount|currency:d.payment.currency}}</h1><span>{{label(d.payment.paymentStatus)}} · {{d.payment.provider}}</span></div><div class="balance"><span>Refunded<strong>{{d.payment.refundedAmount|currency:d.payment.currency}}</strong></span><span>Pending<strong>{{d.payment.pendingRefundAmount|currency:d.payment.currency}}</strong></span><span class="available">Refundable<strong>{{d.payment.refundableAmount|currency:d.payment.currency}}</strong></span></div></header>
<div class="grid"><div class="stack"><article class="card"><h2>Payment context</h2><dl><div><dt>Checkout</dt><dd>{{d.payment.checkoutId}}</dd></div><div><dt>Provider reference</dt><dd>{{d.payment.safeProviderPaymentReference||'Not available'}}</dd></div><div><dt>Buyer</dt><dd>{{d.payment.buyerDisplayName}}</dd></div><div><dt>Businesses</dt><dd>{{d.payment.businessDisplayNames.join(', ')||'Unavailable'}}</dd></div><div><dt>Reconciliation</dt><dd>{{label(d.payment.reconciliationState)}}</dd></div><div><dt>Version</dt><dd>{{d.payment.version}}</dd></div></dl></article>
<article class="card"><h2>Order snapshot</h2>@if(d.orderContext.available){<p><a [routerLink]="d.orderContext.adminPath">{{d.orderContext.orderNumber}}</a> · {{d.orderContext.totalAmount|currency:d.orderContext.currency}}</p><ul>@for(item of d.orderContext.purchaseItems;track item.listingId){<li><span>{{item.titleAtPurchase}} × {{item.quantity}}</span><strong>{{item.lineTotal|currency:d.orderContext.currency}}</strong></li>}</ul>}@else{<p>Order context is temporarily unavailable. Refund execution is disabled until it can be verified.</p>}</article>
<article class="card"><h2>Refund history</h2>@for(r of d.refunds;track r.refundId){<a class="refund" [routerLink]="['/admin/refunds',r.refundId]"><span>{{r.createdAt|date:'short'}} · {{label(r.source)}}</span><strong>{{r.amount|currency:r.currency}} · {{label(r.status)}}</strong></a>}@empty{<p>No refunds have been recorded.</p>}</article>
<article class="card"><h2>Audit timeline</h2>@for(e of d.timeline;track e.eventId){<div class="event"><time>{{e.occurredAt|date:'medium'}}</time><strong>{{label(e.eventType)}}</strong><span>{{e.actorDisplayName||label(e.actorType)}}</span><p>{{e.reason}}</p></div>}@empty{<p>No financial events have been recorded.</p>}</article></div>
<aside class="stack"><article class="card refund-panel"><p class="kicker">Controlled action</p><h2>Issue a refund</h2>@if(!d.capabilities.canExecuteRefund){<p class="muted">{{d.capabilities.readOnlyReason||'Your role has read-only refund access.'}}</p>}@else if(!d.capabilities.isRefundable){<p class="muted">This payment has no refundable balance.</p>}@else{<label>Refund type<select [(ngModel)]="refundType" (ngModelChange)="invalidatePreview()"><option value="FULL">Full remaining balance</option><option value="PARTIAL">Partial amount</option></select></label><label>Amount<input type="number" min="0.01" step="0.01" [(ngModel)]="amount" [disabled]="refundType==='FULL'" (ngModelChange)="invalidatePreview()"></label><label>Dispute recommendation<select [(ngModel)]="disputeId" (ngModelChange)="useRecommendation()"><option value="">None</option>@for(item of d.disputeRecommendations;track item.disputeId){<option [value]="item.disputeId">{{item.disputeId}} · {{item.recommendedRefundAmount|currency:item.currency}}</option>}</select></label><label>Reason code<input [(ngModel)]="reasonCode" (ngModelChange)="invalidatePreview()"></label><label>Operator note<textarea rows="4" [(ngModel)]="reason" (ngModelChange)="invalidatePreview()"></textarea></label><button class="preview" (click)="previewRefund()" [disabled]="working()">Review refund</button>}
@if(preview();as p){<div class="preview-box"><h3>Confirmation ledger</h3><dl><div><dt>Captured</dt><dd>{{p.capturedAmount|currency:p.currency}}</dd></div><div><dt>Already refunded</dt><dd>{{p.alreadyRefundedAmount|currency:p.currency}}</dd></div><div><dt>This refund</dt><dd>{{p.requestedAmount|currency:p.currency}}</dd></div><div class="projected"><dt>Remaining after refund</dt><dd>{{p.projectedRemainingRefundableAmount|currency:p.currency}}</dd></div></dl>@if(p.warnings.length){<ul>@for(w of p.warnings;track w){<li>{{w}}</li>}</ul>}@if(!p.allowed){<p class="alert">{{p.denialReason}}</p>}@else{<label class="confirm"><input type="checkbox" [(ngModel)]="confirmed"> I confirm this exact amount and understand the operation may move money.</label><button class="execute" (click)="executeRefund()" [disabled]="!confirmed||working()">Issue {{p.requestedAmount|currency:p.currency}} refund</button>}</div>}
@if(result();as r){<div class="result"><strong>{{label(r.status)}}</strong><span>Refund {{r.refundId}}</span><a [routerLink]="['/admin/refunds',r.refundId]">Open refund record</a></div>}@if(approval();as a){<div class="result"><strong>Independent approval required</strong><span>{{a.message}}</span><a [routerLink]="['/admin/governance/approvals',a.approval.approvalId]">Open approval {{a.approval.approvalId}}</a></div>}</article></aside></div>}</section>`,styles:[`.page{display:grid;gap:1.2rem}.page>a{color:var(--color-info)}header{display:flex;justify-content:space-between;gap:2rem;align-items:end;padding:1.3rem 0;border-bottom:1px solid var(--color-border)}header p{margin:0;font:700 .78rem/1.2 ui-monospace,monospace;color:var(--color-text-muted)}h1{margin:.25rem 0;font-size:clamp(2.4rem,6vw,4.8rem);letter-spacing:-.05em}.balance{display:flex;border:1px solid var(--color-border)}.balance span{display:grid;padding:.8rem 1rem;border-left:1px solid var(--color-border);color:var(--color-text-muted)}.balance strong{color:var(--color-text-primary);font-size:1.1rem}.balance .available{background:rgba(56,189,248,.08)}.grid{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(320px,.8fr);gap:1.2rem}.stack{display:grid;gap:1rem;align-content:start}.card{padding:1rem;border:1px solid var(--color-border);background:var(--color-bg-secondary)}h2,h3{margin-top:0}dl{display:grid;gap:.6rem}dl div{display:flex;justify-content:space-between;gap:1rem;border-bottom:1px solid var(--color-border);padding-bottom:.45rem}dt{color:var(--color-text-muted)}dd{margin:0;text-align:right;font-weight:750}ul{padding:0;list-style:none}li,.refund{display:flex;justify-content:space-between;gap:1rem;padding:.55rem 0;border-bottom:1px solid var(--color-border)}.refund{color:var(--color-text-primary);text-decoration:none}.event{display:grid;grid-template-columns:9rem 1fr auto;gap:.5rem;padding:.65rem 0;border-bottom:1px solid var(--color-border)}.event p{grid-column:2/-1;margin:0;color:var(--color-text-muted)}.kicker{font:800 .72rem/1 ui-monospace,monospace;letter-spacing:.12em;text-transform:uppercase;color:var(--color-info)}.refund-panel label{display:grid;gap:.35rem;margin:.8rem 0;font-weight:750}input,select,textarea,button{padding:.65rem;border:1px solid var(--color-border);background:var(--color-bg-primary);color:var(--color-text-primary)}button{width:100%;font-weight:850}.preview{border-color:var(--color-info)}.execute{background:var(--color-danger);color:white;border-color:var(--color-danger)}.preview-box{margin-top:1rem;padding:1rem;border:2px solid var(--color-info);background:var(--color-bg-primary)}.projected{font-size:1.1rem}.confirm{grid-template-columns:auto 1fr!important;align-items:start}.confirm input{margin:.2rem 0}.result,.alert{padding:1rem;border:1px solid var(--color-danger);display:grid;gap:.3rem}.result{margin-top:1rem;border-color:var(--color-success)}.muted{color:var(--color-text-muted)}@media(max-width:900px){header{align-items:start;flex-direction:column}.balance{width:100%;overflow:auto}.grid{grid-template-columns:1fr}.page,.grid,.stack,.card,aside,header,header>div{min-width:0}dl div{align-items:start;flex-direction:column;gap:.2rem}dd{max-width:100%;text-align:left;overflow-wrap:anywhere}li,.refund{flex-wrap:wrap}.event{grid-template-columns:1fr}}`]})
export class AdminPaymentDetailComponent implements OnInit{private readonly route=inject(ActivatedRoute);private readonly service=inject(AdminFinanceService);readonly detail=signal<AdminPaymentDetail|null>(null);readonly preview=signal<AdminRefundPreview|null>(null);readonly result=signal<AdminRefundExecution|null>(null);readonly approval=signal<AdminRefundApproval|null>(null);readonly error=signal('');readonly working=signal(false);private id='';refundType:'FULL'|'PARTIAL'='FULL';amount:number|null=null;disputeId='';reasonCode='CUSTOMER_REMEDIATION';reason='';confirmed=false;ngOnInit(){this.id=this.route.snapshot.paramMap.get('paymentId')||'';this.load();}load(){this.service.payment(this.id).subscribe({next:d=>{this.detail.set(d);this.amount=d.payment.refundableAmount;},error:e=>this.error.set(e?.error?.error?.message||'Payment could not be loaded.')});}invalidatePreview(){this.preview.set(null);this.confirmed=false;}useRecommendation(){const d=this.detail();const rec=d?.disputeRecommendations.find(x=>x.disputeId===this.disputeId);if(rec){this.refundType='PARTIAL';this.amount=rec.recommendedRefundAmount;}this.invalidatePreview();}previewRefund(){const d=this.detail();if(!d)return;if(this.refundType==='PARTIAL'&&(this.amount==null||!Number.isFinite(this.amount)||this.amount<=0)){this.invalidatePreview();this.error.set('Enter a partial refund amount greater than zero.');return;}this.working.set(true);this.error.set('');this.service.preview(this.id,this.request(d.payment.version)).subscribe({next:p=>{this.preview.set(p);this.working.set(false);},error:e=>{this.error.set(e?.error?.error?.message||'Refund review failed.');this.working.set(false);}});}executeRefund(){const p=this.preview();if(!p||!this.confirmed)return;this.working.set(true);this.service.execute(this.id,this.request(p.expectedPaymentVersion)).subscribe({next:r=>{if('approvalRequired'in r){this.approval.set(r);this.result.set(null);}else{this.result.set(r);this.approval.set(null);this.load();}this.preview.set(null);this.confirmed=false;this.working.set(false);},error:e=>{if(e.status===409){this.invalidatePreview();this.load();}this.error.set(e?.error?.error?.message||'Refund execution failed.');this.working.set(false);}});}private request(version:number):AdminRefundRequest{return{refundType:this.refundType,amount:this.refundType==='FULL'?null:this.amount,currency:this.detail()?.payment.currency||'USD',reasonCode:this.reasonCode,reason:this.reason,disputeId:this.disputeId||null,expectedPaymentVersion:version,idempotencyKey:`admin-refund:${this.id}:${Date.now()}:${Math.random().toString(36).slice(2)}`};}label(v:string){return(v||'').toLowerCase().replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());}}
