import { CurrencyPipe, DatePipe } from '@angular/common';
import { Component, OnInit, inject, signal } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { AdminRefundDetail } from '../../core/models/admin-finance.model';
import { AdminFinanceService } from '../../core/services/admin-finance.service';

@Component({selector:'app-admin-refund-detail',standalone:true,imports:[RouterLink,CurrencyPipe,DatePipe],template:`<section class="page"><a routerLink="/admin/refunds">← Refunds</a>@if(error()){<p class="alert" role="alert">{{error()}}</p>}@else if(!detail()){<p class="card">Loading refund record…</p>}@else if(detail();as d){<header><div><p>Refund {{d.refund.refundId}}</p><h1>{{d.refund.amount|currency:d.refund.currency}}</h1><span>{{label(d.refund.status)}} · {{label(d.refund.source)}}</span></div><a [routerLink]="['/admin/payments',d.refund.paymentId]">Open payment</a></header><div class="metrics"><span>Original payment<strong>{{d.originalPaymentAmount|currency:d.refund.currency}}</strong></span><span>Refunded before<strong>{{d.refundedBeforeOperation|currency:d.refund.currency}}</strong></span><span>Refunded total<strong>{{d.refundedTotal|currency:d.refund.currency}}</strong></span><span>Currently refundable<strong>{{d.currentlyRefundableAmount|currency:d.refund.currency}}</strong></span></div><div class="grid"><article class="card"><h2>Operation</h2><dl><div><dt>Payment</dt><dd>{{d.refund.paymentId}}</dd></div><div><dt>Order</dt><dd><a [routerLink]="['/admin/orders',d.refund.orderId]">{{d.refund.orderId}}</a></dd></div><div><dt>Dispute</dt><dd>@if(d.refund.disputeId){<a [routerLink]="['/admin/disputes',d.refund.disputeId]">{{d.refund.disputeId}}</a>}@else{None}</dd></div><div><dt>Provider</dt><dd>{{d.refund.provider}}</dd></div><div><dt>Provider reference</dt><dd>{{d.refund.safeProviderRefundReference||'Not assigned'}}</dd></div><div><dt>Reconciliation</dt><dd>{{label(d.reconciliationState)}}</dd></div><div><dt>Initiated by</dt><dd>{{label(d.refund.initiatedByType)}} {{d.refund.initiatedByAdminId||''}}</dd></div><div><dt>Created</dt><dd>{{d.refund.createdAt|date:'medium'}}</dd></div></dl>@if(d.safeFailureSummary){<p class="alert"><strong>{{d.safeFailureCode}}</strong><br>{{d.safeFailureSummary}}</p>}@if(!d.capabilities.canRetryRefund&&d.refund.status==='FAILED'){<p class="notice">Automated retry is unavailable because the provider has no safe reconciliation contract. Review provider state before any further action.</p>}</article><article class="card"><h2>Latest attempt</h2>@if(d.latestAttempt;as a){<dl><div><dt>Attempt</dt><dd>#{{a.attemptNumber}}</dd></div><div><dt>Operation</dt><dd>{{label(a.operation)}}</dd></div><div><dt>Outcome</dt><dd>{{label(a.outcome)}}</dd></div><div><dt>At</dt><dd>{{a.createdAt|date:'medium'}}</dd></div></dl>}@else{<p>No attempt record is available.</p>}<h2>Audit timeline</h2>@for(e of d.timeline;track e.eventId){<div class="event"><time>{{e.occurredAt|date:'medium'}}</time><strong>{{label(e.eventType)}}</strong><span>{{e.actorDisplayName||label(e.actorType)}}</span><p>{{e.reason}}</p></div>}</article></div>}</section>`,styles:[`.page{display:grid;gap:1.2rem}.page>a,header>a,a{color:var(--color-info)}header{display:flex;align-items:end;justify-content:space-between;border-bottom:1px solid var(--color-border);padding:1rem 0}header p{margin:0;font:700 .78rem ui-monospace,monospace;color:var(--color-text-muted)}h1{font-size:clamp(2.4rem,6vw,4.8rem);letter-spacing:-.05em;margin:.2rem 0}.metrics{display:grid;grid-template-columns:repeat(4,1fr);border:1px solid var(--color-border)}.metrics span{display:grid;padding:1rem;border-left:1px solid var(--color-border);color:var(--color-text-muted)}.metrics strong{font-size:1.15rem;color:var(--color-text-primary)}.grid{display:grid;grid-template-columns:1fr 1fr;gap:1rem}.card{padding:1rem;border:1px solid var(--color-border);background:var(--color-bg-secondary)}h2{margin:0 0 1rem}dl{display:grid;gap:.65rem}dl div{display:flex;justify-content:space-between;gap:1rem;padding-bottom:.5rem;border-bottom:1px solid var(--color-border)}dt{color:var(--color-text-muted)}dd{margin:0;text-align:right;overflow-wrap:anywhere}.event{display:grid;grid-template-columns:9rem 1fr auto;gap:.5rem;padding:.65rem 0;border-bottom:1px solid var(--color-border)}.event p{grid-column:2/-1;margin:0;color:var(--color-text-muted)}.alert,.notice{padding:1rem;border:1px solid var(--color-danger)}.notice{border-color:var(--color-border);color:var(--color-text-muted)}@media(max-width:800px){header{align-items:start;flex-direction:column}.metrics,.grid{grid-template-columns:1fr}.metrics span{border-left:0;border-bottom:1px solid var(--color-border)}.event{grid-template-columns:1fr}}`]})
export class AdminRefundDetailComponent implements OnInit{private readonly route=inject(ActivatedRoute);private readonly service=inject(AdminFinanceService);readonly detail=signal<AdminRefundDetail|null>(null);readonly error=signal('');ngOnInit(){const id=this.route.snapshot.paramMap.get('refundId')||'';this.service.refund(id).subscribe({next:v=>this.detail.set(v),error:e=>this.error.set(e?.error?.error?.message||'Refund could not be loaded.')});}label(v:string){return(v||'').toLowerCase().replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());}}
