import { HttpErrorResponse } from '@angular/common/http';
import { Component, OnInit, computed, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, RouterLink } from '@angular/router';
import {
  AdminBusinessDetail, AdminBusinessTimelineEntry, BusinessEnforcementAction, BusinessEnforcementActionType,
  BusinessEnforcementPreview, BusinessEnforcementScope, CreateBusinessEnforcementRequest,
  RevokeBusinessEnforcementRequest,
} from '../../core/models/admin-business.model';
import { AdminBusinessService } from '../../core/services/admin-business.service';
import { AdminService } from '../../core/services/admin.service';
import { ADMIN_PERMISSIONS } from '../../core/security/admin-permissions';

@Component({
  selector:'app-admin-business-detail', standalone:true, imports:[FormsModule,RouterLink],
  styleUrl:'./admin-business-detail.responsive.css',
  template:`<section class="detail"><a class="back" routerLink="/admin/businesses">← Business administration</a>
    @if(loading()){<div class="panel">Loading business…</div>} @else if(error()){<div class="panel error" role="alert">{{error()}} <button (click)="load()">Retry</button></div>}
    @else if(business(); as current){
      <header class="hero"><div><p>Active business control</p><h2>{{current.displayName}}</h2><span>{{current.businessId}}</span></div><div><small>Business state</small><strong>{{current.businessState}}</strong><small>Enforcement · {{current.strongestActiveAction || 'CLEAR'}}</small></div></header>
      <div class="grid two"><article class="panel"><h3>Business &amp; verification</h3><dl><div><dt>Legal name</dt><dd>{{current.legalName}}</dd></div><div><dt>Store state</dt><dd>{{current.storeState}}</dd></div><div><dt>Verification state</dt><dd>{{current.verificationState}}</dd></div><div><dt>Verification reference</dt><dd>{{current.verificationReference}}</dd></div><div><dt>Updated</dt><dd>{{formatDate(current.updatedAt)}} · v{{current.version}}</dd></div></dl></article>
        <article class="panel"><h3>Owner &amp; members</h3>@if(current.ownerSummary;as owner){<p><strong>{{owner.safeDisplayName}}</strong><small>{{owner.userId}} · membership {{owner.membershipState}} · user enforcement {{owner.strongestUserEnforcement || 'CLEAR'}}</small></p>}@for(member of current.membershipSummaries;track member.userId){<div class="member"><strong>{{member.safeDisplayName}}</strong><span>{{member.role}} · membership {{member.membershipState}} · user enforcement {{member.strongestUserEnforcement || 'CLEAR'}}</span></div>}</article>
      </div>
      <div class="grid two"><article class="panel"><h3>Listing summary</h3><div class="metrics"><span><b>{{current.listingSummary.draftCount}}</b>Draft</span><span><b>{{current.listingSummary.pendingReviewCount}}</b>Pending</span><span><b>{{current.listingSummary.activeCount}}</b>Active</span><span><b>{{current.listingSummary.pausedCount}}</b>Paused</span><span><b>{{current.listingSummary.removedCount}}</b>Removed</span></div><a routerLink="/admin/listings/moderation">Open listing administration</a></article>
        <article class="panel"><h3>Orders</h3><p class="muted">{{current.orderSummary.note}}</p><div class="preserved">Existing orders remain accessible and fulfillable. This workflow controls only new marketplace commitments.</div></article></div>
      <article class="panel ledger"><header><div><p>Effective policy</p><h3>Operational capability ledger</h3></div><span>Business enforcement is separate from member enforcement.</span></header><div class="scopes">@for(scope of operationalScopes();track scope){<div><span>{{scopeLabel(scope)}}</span>@if(restrictionFor(scope);as restriction){<strong class="blocked">Blocked · {{restriction.actionType}}</strong>}@else{<strong class="allowed">Allowed</strong>}</div>}<div class="reserved"><span>Business payouts</span><strong>Reserved · unavailable</strong></div></div></article>
      <div class="grid two"><article class="panel"><h3>Active enforcement</h3>@if(!current.activeEnforcementActions.length){<p class="muted">No active business enforcement.</p>}@for(action of current.activeEnforcementActions;track action.enforcementActionId){<div class="action"><div><strong>{{action.actionType==='BAN'?'Marketplace ban':action.actionType}}</strong><span>{{scopeList(action.scopes)}}</span><small>{{action.reasonCode}} · {{action.reason}} · {{action.expiresAt?formatDate(action.expiresAt):'indefinite'}}</small></div>@if(current.availableAdminCapabilities.canReinstate){<button class="secondary" (click)="beginRevoke(action)">Reinstate</button>}</div>}</article>
        <article class="panel"><h3>Historical enforcement</h3>@if(!current.historicalEnforcementActions.length){<p class="muted">No expired or revoked actions.</p>}@for(action of current.historicalEnforcementActions;track action.enforcementActionId){<div class="history"><strong>{{action.actionType}}</strong><span>{{action.lifecycleState}} · {{scopeList(action.scopes)}}</span></div>}</article></div>
      @if(canMutate()) {<article class="panel workbench"><header><div><p>Administrative action</p><h3>Apply business marketplace enforcement</h3></div></header><div class="form">
        <label>Action type<select [(ngModel)]="actionType" (ngModelChange)="actionChanged()"><option value="RESTRICT" [disabled]="!current.availableAdminCapabilities.canRestrict">Restrict</option><option value="SUSPEND" [disabled]="!current.availableAdminCapabilities.canSuspend">Suspend</option><option value="BAN" [disabled]="!current.availableAdminCapabilities.canBan">Marketplace ban</option></select></label>
        <fieldset><legend>Operational scopes</legend><label><input type="checkbox" [(ngModel)]="creation" [disabled]="actionType==='BAN'" (ngModelChange)="invalidate()">Listing creation</label><label><input type="checkbox" [(ngModel)]="publication" [disabled]="actionType==='BAN'" (ngModelChange)="invalidate()">Publication</label><label><input type="checkbox" [(ngModel)]="sales" [disabled]="actionType==='BAN'" (ngModelChange)="invalidate()">New sales</label></fieldset>
        <label>Reason code<input [(ngModel)]="reasonCode" (ngModelChange)="invalidate()" placeholder="POLICY_VIOLATION"></label><label class="wide">Required explanation<textarea [(ngModel)]="reason" (ngModelChange)="invalidate()" rows="3"></textarea></label><label>Expiration (optional)<input type="datetime-local" [(ngModel)]="expiresAt" (ngModelChange)="invalidate()"></label>@if(actionType==='SUSPEND'&&!expiresAt){<label class="confirm"><input type="checkbox" [(ngModel)]="indefiniteConfirmed"> I confirm this suspension is indefinite.</label>}</div>
        <div class="buttons"><button [disabled]="previewing()||!canPreview()" (click)="previewCreate()">{{previewing()?'Checking…':'Preview action'}}</button></div>@if(actionError()){<div class="error" role="alert">{{actionError()}}</div>}
        @if(createPreview();as preview){<section class="preview"><h4>Dry-run impact</h4>@for(line of preview.impactSummary;track line){<p>{{line}}</p>}@for(warning of preview.warnings;track warning){<div class="warning">{{warning}}</div>}<p><strong>Effective after confirmation:</strong> {{effectiveList(preview)}}</p><button [disabled]="saving()||!preview.targetVersionCurrent" (click)="confirmCreate()">Confirm enforcement</button></section>}
      </article>}
      @if(revokeTarget();as target){<article class="panel workbench"><header><h3>Reinstate selected capabilities</h3><button class="text" (click)="cancelRevoke()">Close</button></header><p>Revokes only action {{target.enforcementActionId}}. Other overlapping actions remain effective.</p><label>Reinstatement reason<textarea [(ngModel)]="revokeReason" (ngModelChange)="revokePreview.set(null)" rows="3"></textarea></label><div class="buttons"><button [disabled]="previewing()||!revokeReason.trim()" (click)="previewRevocation()">Preview reinstatement</button></div>@if(revokePreview();as preview){<section class="preview"><p><strong>Restrictions remaining:</strong> {{effectiveList(preview)}}</p>@for(line of preview.impactSummary;track line){<p>{{line}}</p>}<button [disabled]="saving()" (click)="confirmRevoke()">Confirm reinstatement</button></section>}</article>}
      @if(admin.hasPermission(permissions.AUDIT_READ)){<article class="panel"><h3>Audit timeline</h3>@if(!timeline().length){<p class="muted">No business administration events.</p>}@for(event of timeline();track event.eventId){<div class="timeline"><span></span><div><strong>{{eventLabel(event)}}</strong><p>{{event.actionType?scopeList(event.scopes):event.previousState+' → '+event.newState}}</p><small>{{event.actorDisplayName}} · {{formatDate(event.occurredAt)}} · {{event.reasonCode || event.reason || ''}}</small></div></div>}</article>}
    }</section>`,
  styles:[`.detail{display:grid;gap:1rem}.back{color:var(--color-text-muted);font-size:.8rem;text-decoration:none}.panel,.hero{padding:1rem;border:1px solid var(--color-border);border-radius:var(--radius-md);background:rgba(18,19,23,.92)}.hero{display:flex;justify-content:space-between;align-items:end;background:linear-gradient(120deg,rgba(56,189,248,.11),rgba(18,19,23,.92) 52%)}.hero p,.ledger header p,.workbench header p{margin:0;color:var(--color-info);font-size:.7rem;font-weight:850;text-transform:uppercase;letter-spacing:.08em}.hero h2{margin:.2rem 0}.hero span,.hero small,.muted,small{color:var(--color-text-muted);font-size:.7rem}.hero>div:last-child{display:grid;text-align:right;gap:.2rem}.hero>div:last-child strong{color:#fdba74}.grid{display:grid;gap:1rem}.two{grid-template-columns:1fr 1fr}.panel h3{margin:0 0 .75rem}.panel a{color:var(--color-info);font-size:.78rem}dl{display:grid;gap:.5rem;margin:0}dl div{display:flex;justify-content:space-between;border-bottom:1px solid var(--color-border);padding-bottom:.4rem}dt{color:var(--color-text-muted);font-size:.75rem}dd{margin:0;font-size:.78rem}.member,.action,.history{display:flex;justify-content:space-between;align-items:center;gap:.7rem;padding:.6rem 0;border-top:1px solid var(--color-border)}.member{display:grid;gap:.15rem}.member span,.action span,.history span{color:var(--color-text-secondary);font-size:.75rem}.action>div{display:grid;gap:.15rem}.metrics{display:grid;grid-template-columns:repeat(5,1fr);gap:.4rem;margin-bottom:.7rem}.metrics span{display:grid;gap:.2rem;text-align:center;color:var(--color-text-muted);font-size:.67rem}.metrics b{color:var(--color-text-primary);font-size:1.15rem}.preserved{padding:.65rem;border-left:3px solid #4ade80;background:rgba(74,222,128,.06);color:#bbf7d0;font-size:.76rem}.ledger header,.workbench header{display:flex;justify-content:space-between;align-items:start}.ledger header span{color:var(--color-text-muted);font-size:.7rem}.scopes{display:grid;grid-template-columns:repeat(4,1fr);gap:.6rem}.scopes>div{display:grid;gap:.35rem;padding:.65rem;border:1px solid var(--color-border);border-radius:var(--radius-md);font-size:.75rem}.allowed{color:#86efac}.blocked{color:#fca5a5}.reserved strong{color:var(--color-text-muted)}button,input,select,textarea{border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-bg-primary);color:var(--color-text-primary);padding:.55rem;font:inherit}button{border-color:var(--color-info);color:var(--color-info);font-weight:800;cursor:pointer}button:disabled{opacity:.4}.secondary{border-color:var(--color-border);color:var(--color-text-secondary)}.text{border:0;background:transparent}.form{display:grid;grid-template-columns:1fr 1fr;gap:.75rem}.form label,.workbench>label{display:grid;gap:.3rem;color:var(--color-text-muted);font-size:.73rem;font-weight:750}.form .wide{grid-column:1/-1}fieldset{display:flex;flex-wrap:wrap;gap:.5rem;border:1px solid var(--color-border);border-radius:var(--radius-md)}fieldset label,.confirm{display:flex!important;align-items:center;gap:.3rem}.buttons{display:flex;justify-content:flex-end;margin-top:.75rem}.preview{margin-top:.8rem;padding:.9rem;border-left:3px solid var(--color-info);background:rgba(56,189,248,.06)}.preview h4{margin:0}.preview p{font-size:.77rem;color:var(--color-text-secondary)}.warning,.error{color:#fde68a;font-size:.78rem}.error{color:var(--color-danger)}.timeline{display:grid;grid-template-columns:10px 1fr;gap:.55rem;padding:.55rem 0}.timeline>span{width:8px;height:8px;margin-top:.35rem;border-radius:50%;background:var(--color-info)}.timeline p{margin:.15rem 0;color:var(--color-text-secondary);font-size:.75rem}@media(max-width:800px){.two,.scopes{grid-template-columns:1fr 1fr}.form{grid-template-columns:1fr}}@media(max-width:560px){.two,.scopes{grid-template-columns:1fr}.hero{align-items:start;flex-direction:column;gap:.8rem}.hero>div:last-child{text-align:left}.metrics{grid-template-columns:repeat(3,1fr)}}`]
})
export class AdminBusinessDetailComponent implements OnInit{
  private readonly route=inject(ActivatedRoute);private readonly service=inject(AdminBusinessService);readonly admin=inject(AdminService);readonly permissions=ADMIN_PERMISSIONS;
  readonly business=signal<AdminBusinessDetail|null>(null);readonly timeline=signal<AdminBusinessTimelineEntry[]>([]);readonly loading=signal(true);readonly error=signal('');readonly actionError=signal('');readonly previewing=signal(false);readonly saving=signal(false);readonly createPreview=signal<BusinessEnforcementPreview|null>(null);readonly revokePreview=signal<BusinessEnforcementPreview|null>(null);readonly revokeTarget=signal<BusinessEnforcementAction|null>(null);
  readonly operationalScopes=computed(()=>this.business()?.availableAdminCapabilities.operationalScopes??[]);actionType:BusinessEnforcementActionType='RESTRICT';creation=true;publication=false;sales=false;reasonCode='';reason='';expiresAt='';indefiniteConfirmed=false;revokeReason='';private key:string|null=null;private revokeKey:string|null=null;private readonly id=this.route.snapshot.paramMap.get('businessId')??'';
  ngOnInit(){this.load();}load(){this.loading.set(true);this.error.set('');this.service.detail(this.id).subscribe({next:r=>{this.business.set(r.data);this.loading.set(false);this.loadTimeline();},error:r=>{this.error.set(r.status===403?'You do not have permission to read this business.':'Business detail could not be loaded.');this.loading.set(false);}});}private loadTimeline(){if(!this.admin.hasPermission(this.permissions.AUDIT_READ))return;this.service.timeline(this.id).subscribe({next:r=>this.timeline.set(r.data),error:()=>this.timeline.set([])});}
  canMutate(){const c=this.business()?.availableAdminCapabilities;return !!c&&(c.canRestrict||c.canSuspend||c.canBan); }canAction(){const c=this.business()?.availableAdminCapabilities;return this.actionType==='RESTRICT'?!!c?.canRestrict:this.actionType==='SUSPEND'?!!c?.canSuspend:!!c?.canBan;}canPreview(){return this.canAction()&&this.selectedScopes().length>0&&!!this.reasonCode.trim()&&!!this.reason.trim()&&!(this.actionType==='SUSPEND'&&!this.expiresAt&&!this.indefiniteConfirmed);}
  actionChanged(){if(this.actionType==='BAN'){this.creation=this.publication=this.sales=true;}this.invalidate();}invalidate(){this.createPreview.set(null);this.key=null;this.actionError.set('');}
  previewCreate(){const body=this.createBody(null);if(!body)return;this.previewing.set(true);this.service.previewCreate(this.id,body).subscribe({next:r=>{this.createPreview.set(r.data);this.key=this.newKey();this.previewing.set(false);},error:r=>{this.actionError.set(this.message(r,'The action could not be previewed.'));this.previewing.set(false);}});}confirmCreate(){const body=this.createBody(this.key);if(!body||!this.createPreview())return;this.saving.set(true);this.service.create(this.id,body).subscribe({next:()=>this.afterMutation(),error:r=>this.failed(r)});}
  beginRevoke(a:BusinessEnforcementAction){this.revokeTarget.set(a);this.revokeReason='';this.revokePreview.set(null);this.revokeKey=null;}cancelRevoke(){this.revokeTarget.set(null);this.revokePreview.set(null);}previewRevocation(){const a=this.revokeTarget();if(!a?.enforcementActionId)return;this.previewing.set(true);this.service.previewRevoke(this.id,a.enforcementActionId,this.revokeBody(a,null)).subscribe({next:r=>{this.revokePreview.set(r.data);this.revokeKey=this.newKey();this.previewing.set(false);},error:r=>{this.actionError.set(this.message(r,'Reinstatement could not be previewed.'));this.previewing.set(false);}});}confirmRevoke(){const a=this.revokeTarget();if(!a?.enforcementActionId||!this.revokePreview())return;this.saving.set(true);this.service.revoke(this.id,a.enforcementActionId,this.revokeBody(a,this.revokeKey)).subscribe({next:()=>this.afterMutation(),error:r=>this.failed(r)});}
  private afterMutation(){this.saving.set(false);this.createPreview.set(null);this.cancelRevoke();this.load();}private failed(r:HttpErrorResponse){this.saving.set(false);this.actionError.set(this.message(r,'The enforcement action could not be completed.'));if(r.status===409){this.createPreview.set(null);this.revokePreview.set(null);this.load();}}
  private createBody(key:string|null):CreateBusinessEnforcementRequest|null{const b=this.business();if(!b)return null;let expiration:string|null=null;if(this.expiresAt){const d=new Date(this.expiresAt);if(Number.isNaN(d.getTime())){this.actionError.set('Expiration is invalid.');return null;}expiration=d.toISOString();}return{actionType:this.actionType,scopes:this.selectedScopes(),reasonCode:this.reasonCode.trim().toUpperCase(),reason:this.reason.trim(),effectiveAt:null,expiresAt:expiration,expectedBusinessVersion:b.version,idempotencyKey:key,safeMetadata:{origin:'admin-business-detail'}};}
  private revokeBody(a:BusinessEnforcementAction,key:string|null):RevokeBusinessEnforcementRequest{return{expectedEnforcementVersion:a.version,reasonCode:'ADMIN_REINSTATEMENT',reason:this.revokeReason.trim(),idempotencyKey:key,safeMetadata:{origin:'admin-business-detail'}};}private selectedScopes():BusinessEnforcementScope[]{const available=this.operationalScopes();if(this.actionType==='BAN')return[...available];return[...(this.creation&&available.includes('BUSINESS_LISTING_CREATION')?['BUSINESS_LISTING_CREATION' as const]:[]),...(this.publication&&available.includes('BUSINESS_LISTING_PUBLICATION')?['BUSINESS_LISTING_PUBLICATION' as const]:[]),...(this.sales&&available.includes('BUSINESS_NEW_SALES')?['BUSINESS_NEW_SALES' as const]:[])];}
  restrictionFor(s:BusinessEnforcementScope){return this.business()?.effectiveRestrictions.find(r=>r.scope===s);}scopeLabel(s:BusinessEnforcementScope){return s==='BUSINESS_LISTING_CREATION'?'Listing creation':s==='BUSINESS_LISTING_PUBLICATION'?'Listing publication':'New sales';}scopeList(scopes:BusinessEnforcementScope[]){return scopes.map(s=>this.scopeLabel(s)).join(', ')||'none';}effectiveList(p:BusinessEnforcementPreview){return p.effectiveRestrictionsAfter.length?p.effectiveRestrictionsAfter.map(r=>`${this.scopeLabel(r.scope)} (${r.actionType})`).join(', '):'none';}eventLabel(e:AdminBusinessTimelineEntry){return e.eventType==='REVOKED'?'Enforcement revoked':e.eventType==='CREATED'?'Enforcement created':e.eventType.replaceAll('_',' ').toLowerCase();}formatDate(v:string){return new Date(v).toLocaleString();}private message(r:HttpErrorResponse,f:string){return r.error?.error?.message||f;}private newKey(){return globalThis.crypto?.randomUUID?.()??`admin-business-${Date.now()}-${Math.random().toString(36).slice(2)}`;}
}
