import { DatePipe } from '@angular/common';
import { Component, OnInit, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { Observable } from 'rxjs';
 import { CatalogApproval, CatalogAttribute, CatalogAttributeType, CatalogDetail, CatalogImpact, CatalogOption, CategoryStatus, GuidanceType, SellerEligibility, SellerGuidance } from '../../core/models/catalog.model';
import { CatalogService } from '../../core/services/catalog.service';
import { catalogAdminCapability } from './catalog-admin-capability';

@Component({selector:'app-admin-catalog-detail',standalone:true,imports:[FormsModule,RouterLink,DatePipe],template:`
<section class="detail-page"><a class="back" routerLink="/admin/catalog">← Catalog control room</a>
 @if(error()){<p class="alert" role="alert">{{error()}}</p>}@if(approval();as a){<p class="notice">{{a.message}} <a [routerLink]="['/admin/governance/approvals',a.approval.approvalId]">Open approval {{a.approval.approvalId}}</a></p>}
@if(detail();as d){
  <header class="hero"><div><p class="eyebrow">{{d.breadcrumb.length?path(d):'Root category'}}</p><h1>{{d.category.name}}</h1><p>{{d.category.description||'No category description has been added.'}}</p></div><div class="identity"><span [class]="'status '+d.category.status.toLowerCase()">{{label(d.category.status)}}</span><small>{{d.category.slug}}</small><strong>Rule v{{d.category.ruleVersion}}</strong></div></header>
  @if(cap(d).readOnlyReason){<p class="notice">{{cap(d).readOnlyReason}}</p>}
  <section class="blast"><div><p class="eyebrow">Blast-radius contract</p><h2>Governance changes never cascade into enforcement</h2></div><span>Active listings stay active</span><span>Pending review stays pinned</span><span>Orders remain untouched</span></section>
  <div class="metrics"><article><span>Active listings</span><strong>{{d.category.counts.activeListings}}</strong></article><article><span>Drafts</span><strong>{{d.category.counts.draftListings}}</strong></article><article><span>Pending review</span><strong>{{d.category.counts.pendingListings}}</strong></article><article><span>Historical</span><strong>{{d.category.counts.historicalListings}}</strong></article><article><span>Children</span><strong>{{d.category.counts.childCategories}}</strong></article></div>
  <nav class="tabs"><a href="#summary">Summary</a><a href="#policy">Policy</a><a href="#attributes">Attributes</a><a href="#guidance">Guidance</a><a href="#history">History</a></nav>
  <div class="layout"><main class="stack">
    <article class="card" id="summary"><header><div><p class="eyebrow">Category summary</p><h2>Metadata and hierarchy</h2></div></header><dl><div><dt>Parent</dt><dd>{{parentName(d)}}</dd></div><div><dt>Display order</dt><dd>{{d.category.displayOrder}}</dd></div><div><dt>Seller eligibility</dt><dd>{{label(d.category.sellerEligibility)}}</dd></div><div><dt>Category version</dt><dd>{{d.category.version}}</dd></div></dl>
      @if(cap(d).canManageCategory){<details><summary>Edit metadata</summary><form (ngSubmit)="updateMetadata(d)"><label>Name<input name="editName" [(ngModel)]="editName" maxlength="160"></label><label>Description<textarea name="editDescription" [(ngModel)]="editDescription" maxlength="1000"></textarea></label><label>Display order<input name="editOrder" type="number" min="0" [(ngModel)]="editOrder"></label><label>Reason<input name="editReason" [(ngModel)]="editReason" maxlength="500"></label><button>Save metadata</button></form></details>
      <details><summary>Move category</summary><form (ngSubmit)="previewMove(d)"><label>New parent ID<input name="moveParent" [(ngModel)]="moveParent" placeholder="Blank for root"></label><label>Reason<input name="moveReason" [(ngModel)]="moveReason"></label><button>Preview hierarchy change</button></form></details>}
      @if(d.children.length){<div class="children"><h3>Child categories</h3>@for(child of d.children;track child.id){<a [routerLink]="['/admin/catalog/categories',child.id]"><span>{{child.name}}</span><small>{{label(child.status)}} · {{child.counts.activeListings}} active</small></a>}</div>}
    </article>
    <article class="card" id="policy"><header><div><p class="eyebrow">Listing policy</p><h2>Eligibility and submission controls</h2></div><strong>Rule v{{d.category.ruleVersion}}</strong></header><div class="policy-grid"><span [class.off]="!d.category.listingCreationAllowed">New draft creation <b>{{d.category.listingCreationAllowed?'Allowed':'Blocked'}}</b></span><span [class.off]="!d.category.listingSubmissionAllowed">Submission <b>{{d.category.listingSubmissionAllowed?'Allowed':'Blocked'}}</b></span><span>Seller scope <b>{{label(d.category.sellerEligibility)}}</b></span></div>
      @if(cap(d).canManagePolicy){<form class="policy-form" (ngSubmit)="previewPolicy(d)"><label>Seller eligibility<select name="policyEligibility" [(ngModel)]="policyEligibility"><option>BOTH</option><option>INDIVIDUAL</option><option>BUSINESS</option><option>NONE</option></select></label><label class="check"><input name="creation" type="checkbox" [(ngModel)]="creationAllowed">Allow new drafts</label><label class="check"><input name="submission" type="checkbox" [(ngModel)]="submissionAllowed">Allow submission</label><label>Replacement category ID<input name="replacement" [(ngModel)]="replacementCategoryId" placeholder="Optional; no automatic migration"></label><label>Reason<textarea name="policyReason" [(ngModel)]="policyReason"></textarea></label><button>Dry-run policy change</button></form>}
    </article>
    <article class="card" id="attributes"><header><div><p class="eyebrow">Authoritative schema</p><h2>Listing attributes</h2><p>Required fields block submission. Searchable/filterable flags are stored for later index integration.</p></div><strong>{{d.attributes.length}}</strong></header>
      <div class="attribute-list">@for(a of d.attributes;track a.id){<section class="attribute"><div><span [class]="'status '+a.status.toLowerCase()">{{label(a.status)}}</span><h3>{{a.label}}</h3><code>{{a.key}}</code><p>{{a.description}}</p></div><dl><div><dt>Type</dt><dd>{{label(a.dataType)}}</dd></div><div><dt>Required</dt><dd>{{a.required?'Yes':'No'}}</dd></div><div><dt>Discovery</dt><dd>{{a.searchable?'Searchable ':''}}{{a.filterable?'Filterable':''}}</dd></div><div><dt>Version</dt><dd>{{a.version}}</dd></div></dl>
        @if(a.options.length){<div class="options">@for(o of a.options;track o.id){<span [class.off]="o.status!=='ACTIVE'">{{o.label}} <small>{{o.value}}</small>@if(cap(d).canManageAttribute&&cap(d).canManagePolicy){<button class="link-button" (click)="previewOptionStatus(d,a.id,o)">{{o.status==='ACTIVE'?'Disable':'Enable'}}</button>}</span>}</div>}
        @if(cap(d).canManageAttribute&&cap(d).canManagePolicy){<div class="actions"><button class="secondary" (click)="editAttribute(d,a)">Edit</button><button class="secondary" (click)="toggleAttribute(d,a.id,a.version,a.status)">{{a.status==='ACTIVE'?'Disable':'Enable'}}</button>@if(a.dataType==='ENUM'||a.dataType==='MULTI_ENUM'){<button class="secondary" (click)="addOption(d,a.id)">Add option</button>}</div>}
      </section>}@if(!d.attributes.length){<p class="empty">No category-specific attributes.</p>}</div>
      @if(cap(d).canManageAttribute&&cap(d).canManagePolicy){<details><summary>Add governed attribute</summary><form class="attribute-form" (ngSubmit)="createAttribute(d)"><label>Stable key<input name="attributeKey" [(ngModel)]="attributeKey" pattern="[a-z][a-z0-9_]+" placeholder="storage_gb"></label><label>Label<input name="attributeLabel" [(ngModel)]="attributeLabel"></label><label>Type<select name="attributeType" [(ngModel)]="attributeType"><option>TEXT</option><option>NUMBER</option><option>BOOLEAN</option><option>ENUM</option><option>MULTI_ENUM</option></select></label><label class="check"><input name="required" type="checkbox" [(ngModel)]="attributeRequired">Required at submission</label><label class="check"><input name="searchable" type="checkbox" [(ngModel)]="attributeSearchable">Searchable metadata</label><label class="check"><input name="filterable" type="checkbox" [(ngModel)]="attributeFilterable">Filterable metadata</label>@if(attributeType==='TEXT'){<label>Minimum length<input name="minLength" type="number" min="0" [(ngModel)]="minLength"></label><label>Maximum length<input name="maxLength" type="number" min="1" max="1000" [(ngModel)]="maxLength"></label>}@if(attributeType==='NUMBER'){<label>Minimum<input name="minimum" type="number" [(ngModel)]="minimum"></label><label>Maximum<input name="maximum" type="number" [(ngModel)]="maximum"></label>}@if(attributeType==='ENUM'||attributeType==='MULTI_ENUM'){<label>Initial options<textarea name="enumOptions" [(ngModel)]="enumOptions" placeholder="black:Black&#10;blue:Blue"></textarea></label>}<label>Reason<input name="attributeReason" [(ngModel)]="attributeReason"></label><button>Publish new attribute rule</button></form></details>}
    </article>
    <article class="card" id="guidance"><header><div><p class="eyebrow">Non-blocking help</p><h2>Seller guidance</h2><p>Guidance is displayed in the listing form and never acts as hidden enforcement.</p></div></header>@for(g of d.sellerGuidance;track g.id){<section class="guidance" [class.off]="g.status!=='ACTIVE'"><div><span>{{label(g.guidanceType)}}</span><h3>{{g.title}}</h3><p>{{g.body}}</p></div>@if(cap(d).canManageGuidance){<div class="actions"><button class="secondary" (click)="editGuidance(g)">Edit</button><button class="secondary" (click)="toggleGuidance(d,g.id,g.version,g.status)">{{g.status==='ACTIVE'?'Disable':'Enable'}}</button></div>}</section>}@if(!d.sellerGuidance.length){<p class="empty">No seller guidance configured.</p>}@if(cap(d).canManageGuidance){<details><summary>Add seller guidance</summary><form (ngSubmit)="createGuidance(d)"><label>Type<select name="guidanceType" [(ngModel)]="guidanceType"><option>TIP</option><option>WARNING</option><option>BEST_PRACTICE</option><option>POLICY_NOTICE</option></select></label><label>Title<input name="guidanceTitle" [(ngModel)]="guidanceTitle"></label><label>Guidance<textarea name="guidanceBody" [(ngModel)]="guidanceBody" maxlength="4000"></textarea></label><label>Reason<input name="guidanceReason" [(ngModel)]="guidanceReason"></label><button>Add non-blocking guidance</button></form></details>}
    </article>
    <article class="card" id="history"><header><div><p class="eyebrow">Traceability</p><h2>Rule versions</h2></div></header>@for(v of d.ruleVersions;track v.id){<div class="timeline-row"><time>{{v.createdAt|date:'medium'}}</time><strong>Rule v{{v.versionNumber}}</strong><span>{{v.reason}}</span></div>}</article>
    <article class="card"><header><div><p class="eyebrow">Immutable audit</p><h2>Catalog timeline</h2></div></header>@for(e of d.auditTimeline;track e.eventId){<div class="timeline-row"><time>{{e.occurredAt|date:'medium'}}</time><strong>{{label(e.eventType)}}</strong><span>{{e.actorDisplayName||e.actorId}} · {{e.reason}}</span></div>}@if(!d.auditTimeline.length){<p class="empty">No catalog events recorded.</p>}</article>
  </main><aside class="stack"><article class="card lifecycle"><p class="eyebrow">Lifecycle action</p><h2>Safe status change</h2><p>Every change is previewed. No listings, orders, or enforcement records are mutated.</p>@if(cap(d).canDisableCategory){<label>Target status<select [(ngModel)]="targetStatus"><option>ACTIVE</option><option>DISABLED</option><option>DEPRECATED</option></select></label><label>Reason<textarea [(ngModel)]="statusReason"></textarea></label><button class="danger" (click)="previewStatus(d)">Dry-run status change</button>}</article>
  <article class="card"><p class="eyebrow">Compatibility</p><h2>Existing content policy</h2><ul><li>Active listings remain discoverable.</li><li>Drafts remain viewable but use the latest rules on save or submit.</li><li>Pending review remains pinned to the captured version.</li><li>Categories are never hard-deleted.</li></ul></article></aside></div>
  @if(preview();as p){<div class="modal-backdrop"><section class="modal" role="dialog" aria-modal="true"><p class="eyebrow">Exact dry-run</p><h2>Review {{label(p.changeType)}}</h2><div class="impact"><span><b>{{p.counts.activeListings}}</b> active</span><span><b>{{p.counts.draftListings}}</b> drafts</span><span><b>{{p.counts.pendingListings}}</b> pending</span><span><b>{{p.listingsMissingRequiredAttribute}}</b> missing required value</span></div><h3>Will happen</h3><ul>@for(item of p.effects;track item){<li>{{item}}</li>}</ul><h3>Guaranteed not to happen</h3><ul class="safe">@for(item of p.guaranteedNonEffects;track item){<li>{{item}}</li>}</ul><div class="actions"><button class="secondary" (click)="cancelPreview()">Cancel</button><button class="danger" [disabled]="saving()" (click)="confirmPreview(d)">Confirm reviewed change</button></div></section></div>}
}@else if(!error()){<p class="loading">Loading catalog policy…</p>}</section>`,styles:[`
:host{display:block}.detail-page{display:grid;gap:1rem;color:var(--color-text-primary)}.back,a{color:var(--color-accent);font-weight:700}.hero{display:flex;justify-content:space-between;gap:2rem;align-items:end;padding:1.5rem;border-radius:18px;border:1px solid var(--color-border);background:linear-gradient(145deg,var(--color-bg-secondary),color-mix(in srgb,var(--color-accent) 9%,var(--color-bg-secondary)))}h1{font-size:clamp(2.1rem,4vw,3.8rem);letter-spacing:-.05em;line-height:.95;margin:.25rem 0 .7rem}.hero p{color:var(--color-text-secondary);max-width:700px}.eyebrow{text-transform:uppercase;letter-spacing:.14em;font-size:.7rem;font-weight:800;color:var(--color-accent)}.identity{display:grid;justify-items:end;gap:.45rem}.status{display:inline-flex;width:max-content;padding:.28rem .5rem;border-radius:999px;background:#284936;color:#9ce5b2;font-size:.7rem;font-weight:800}.status.disabled,.status.deprecated{background:#4a3b28;color:#efc47e}.notice,.alert{padding:.9rem;border:1px solid var(--color-border);border-radius:10px}.alert{border-color:#b94a57}.blast{display:flex;align-items:center;gap:1rem;padding:1rem;border:1px solid #478b68;border-left:5px solid #6ad391;border-radius:12px;background:color-mix(in srgb,#264d36 60%,var(--color-bg-secondary))}.blast div{margin-right:auto}.blast h2{margin:.15rem 0}.blast>span{padding:.5rem;border-radius:8px;background:#173122;font-size:.75rem}.metrics{display:grid;grid-template-columns:repeat(5,1fr);gap:.6rem}.metrics article,.card{border:1px solid var(--color-border);border-radius:14px;background:var(--color-bg-secondary);padding:1rem}.metrics article{display:grid;gap:.45rem}.metrics span,dt{font-size:.75rem;color:var(--color-text-secondary)}.metrics strong{font-size:1.65rem}.tabs{display:flex;gap:.4rem;overflow:auto}.tabs a{padding:.55rem .75rem;border:1px solid var(--color-border);border-radius:999px;text-decoration:none;font-size:.75rem}.layout{display:grid;grid-template-columns:minmax(0,1fr) 280px;gap:1rem}.stack{display:grid;align-content:start;gap:1rem}.card>header{display:flex;justify-content:space-between;gap:1rem}.card h2{margin:.2rem 0}.card header p:not(.eyebrow),.empty{color:var(--color-text-secondary)}dl{display:grid;grid-template-columns:repeat(4,1fr);gap:.5rem}dl div{padding:.65rem;background:var(--color-bg-primary);border-radius:8px}dd{margin:.2rem 0 0;font-weight:700}details{margin-top:1rem;border-top:1px solid var(--color-border);padding-top:.8rem}summary{cursor:pointer;font-weight:800;color:var(--color-accent)}form{display:grid;grid-template-columns:1fr 1fr;gap:.7rem;margin-top:.8rem}label{display:grid;gap:.3rem;color:var(--color-text-secondary);font-size:.75rem}input,select,textarea{box-sizing:border-box;width:100%;padding:.65rem;border:1px solid var(--color-border);border-radius:8px;background:var(--color-bg-primary);color:inherit}textarea{min-height:76px}.check{display:flex;align-items:center;gap:.5rem}.check input{width:auto}button{padding:.65rem .85rem;border:0;border-radius:8px;background:var(--color-accent);color:#07110d;font-weight:800;cursor:pointer}.secondary{background:var(--color-bg-tertiary);color:inherit;border:1px solid var(--color-border)}.link-button{margin-left:.4rem;padding:.15rem .3rem;background:transparent;color:var(--color-accent);font-size:.68rem}.danger{background:#bd5960;color:white}.children{display:grid;gap:.4rem;margin-top:1rem}.children a{display:flex;justify-content:space-between;padding:.7rem;background:var(--color-bg-primary);border-radius:8px;text-decoration:none}.policy-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:.5rem}.policy-grid span{display:grid;gap:.25rem;padding:.7rem;border-left:3px solid #6ad391;background:var(--color-bg-primary)}.policy-grid span.off{border-left-color:#c38a3c}.attribute-list{display:grid;gap:.6rem}.attribute{padding:.85rem;border:1px solid var(--color-border);border-radius:10px;display:grid;grid-template-columns:1.2fr 1fr auto;gap:.8rem}.attribute h3{margin:.3rem 0}.attribute p{color:var(--color-text-secondary)}.attribute dl{grid-template-columns:1fr 1fr;margin:0}.options{grid-column:1/-1;display:flex;flex-wrap:wrap;gap:.4rem}.options span{padding:.35rem .5rem;border-radius:6px;background:var(--color-bg-primary)}.off{opacity:.55}.actions{display:flex;gap:.4rem;align-items:start}.guidance{display:flex;justify-content:space-between;gap:1rem;padding:.8rem;margin:.55rem 0;border-left:3px solid #6ad391;background:var(--color-bg-primary)}.guidance h3{margin:.25rem 0}.timeline-row{display:grid;grid-template-columns:170px 210px 1fr;gap:.6rem;padding:.7rem 0;border-bottom:1px solid var(--color-border);font-size:.78rem}.timeline-row time,.timeline-row span{color:var(--color-text-secondary)}ul{padding-left:1.2rem;color:var(--color-text-secondary);line-height:1.65}.modal-backdrop{position:fixed;inset:0;z-index:1000;display:grid;place-items:center;padding:1rem;background:#050806cc}.modal{max-width:640px;width:100%;max-height:90vh;overflow:auto;padding:1.3rem;border:1px solid var(--color-border);border-radius:16px;background:var(--color-bg-secondary);box-shadow:0 24px 70px #000}.impact{display:grid;grid-template-columns:1fr 1fr;gap:.5rem}.impact span{display:grid;padding:.7rem;background:var(--color-bg-primary);border-radius:8px}.impact b{font-size:1.4rem}.safe li::marker{color:#6ad391}.modal .actions{justify-content:end}.loading{text-align:center;padding:2rem}@media(max-width:1050px){.layout{grid-template-columns:1fr}.metrics{grid-template-columns:repeat(3,1fr)}.blast{align-items:flex-start;flex-wrap:wrap}.attribute{grid-template-columns:1fr}}@media(max-width:650px){.hero{align-items:start;flex-direction:column}.identity{justify-items:start}.metrics{grid-template-columns:1fr 1fr}.policy-grid,form,dl{grid-template-columns:1fr}.timeline-row{grid-template-columns:1fr}.blast>span{width:100%}}
`]})
export class AdminCatalogDetailComponent implements OnInit{
 private readonly route=inject(ActivatedRoute);private readonly service=inject(CatalogService);readonly detail=signal<CatalogDetail|null>(null);readonly approval=signal<CatalogApproval|null>(null);readonly error=signal('');readonly preview=signal<CatalogImpact|null>(null);readonly saving=signal(false);private id='';private pendingMutation:(()=>Observable<CatalogDetail|CatalogApproval>)|null=null;
 editName='';editDescription='';editOrder=0;editReason='Catalog metadata correction';moveParent='';moveReason='Category hierarchy correction';targetStatus:CategoryStatus='DISABLED';statusReason='Marketplace catalog policy update';policyEligibility:SellerEligibility='BOTH';creationAllowed=true;submissionAllowed=true;replacementCategoryId='';policyReason='Reviewed marketplace eligibility update';attributeKey='';attributeLabel='';attributeType:CatalogAttributeType='TEXT';attributeRequired=false;attributeSearchable=false;attributeFilterable=false;minLength=0;maxLength=1000;minimum:number|null=null;maximum:number|null=null;enumOptions='';attributeReason='Reviewed category schema update';guidanceType:GuidanceType='TIP';guidanceTitle='';guidanceBody='';guidanceReason='Seller guidance update';
 ngOnInit(){this.id=this.route.snapshot.paramMap.get('categoryId')||'';this.load();}cap(d:CatalogDetail){return catalogAdminCapability(d.availableAdminCapabilities);}path(d:CatalogDetail){return d.breadcrumb.map(v=>v.name).join(' / ');}parentName(d:CatalogDetail){return d.breadcrumb.length>1?d.breadcrumb[d.breadcrumb.length-2].name:'Root category';}label(v:string){return(v||'').toLowerCase().replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());}
 load(message=''){this.error.set(message);this.service.detail(this.id).subscribe({next:v=>{this.detail.set(v);this.hydrate(v);},error:e=>this.error.set(e?.error?.error?.message||'Category configuration was not found.')});}
 hydrate(d:CatalogDetail){this.editName=d.category.name;this.editDescription=d.category.description||'';this.editOrder=d.category.displayOrder;this.moveParent=d.category.parentId||'';this.targetStatus=d.category.status;this.policyEligibility=d.category.sellerEligibility;this.creationAllowed=d.category.listingCreationAllowed;this.submissionAllowed=d.category.listingSubmissionAllowed;this.replacementCategoryId=d.category.replacementCategoryId||'';}
 updateMetadata(d:CatalogDetail){this.act(this.service.update(this.id,{expectedVersion:d.category.version,name:this.editName,description:this.editDescription||null,displayOrder:this.editOrder,sellerEligibility:d.category.sellerEligibility,reason:this.editReason}));}
 previewMove(d:CatalogDetail){const request={expectedVersion:d.category.version,parentId:this.moveParent||null,reason:this.moveReason};this.pendingMutation=()=>this.service.move(this.id,request);this.service.movePreview(this.id,request).subscribe(this.previewObserver());}
 previewStatus(d:CatalogDetail){const request={expectedVersion:d.category.version,status:this.targetStatus,reason:this.statusReason};this.pendingMutation=()=>this.service.changeStatus(this.id,request);this.service.statusPreview(this.id,request).subscribe(this.previewObserver());}
 previewPolicy(d:CatalogDetail){const request=this.policyRequest(d);this.pendingMutation=()=>this.service.publishPolicy(this.id,request);this.service.policyPreview(this.id,request).subscribe(this.previewObserver());}
 policyRequest(d:CatalogDetail){return{expectedVersion:d.category.version,sellerEligibility:this.policyEligibility,listingCreationAllowed:this.creationAllowed,listingSubmissionAllowed:this.submissionAllowed,replacementCategoryId:this.replacementCategoryId||null,reason:this.policyReason};}
 confirmPreview(_d:CatalogDetail){const request=this.pendingMutation?.();if(request)this.actGoverned(request);}
 cancelPreview(){this.preview.set(null);this.pendingMutation=null;}
 createAttribute(d:CatalogDetail){const request=this.newAttributeRequest(d);this.pendingMutation=()=>this.service.createAttribute(this.id,request);this.service.createAttributePreview(this.id,request).subscribe(this.previewObserver());}
 private newAttributeRequest(d:CatalogDetail){const validation=this.attributeType==='TEXT'?{minLength:this.minLength,maxLength:this.maxLength}:this.attributeType==='NUMBER'?{minimum:this.minimum,maximum:this.maximum}:{};const options=(this.attributeType==='ENUM'||this.attributeType==='MULTI_ENUM')?this.enumOptions.split(/\r?\n/).filter(Boolean).map((line,i)=>{const [value,...label]=line.split(':');return{value:value.trim(),label:(label.join(':')||value).trim(),displayOrder:i};}):[];return{key:this.attributeKey.trim(),label:this.attributeLabel.trim(),description:null,dataType:this.attributeType,required:this.attributeRequired,searchable:this.attributeSearchable,filterable:this.attributeFilterable,displayOrder:d.attributes.length,validation,options,reason:this.attributeReason};}
 editAttribute(_d:CatalogDetail,a:CatalogAttribute){const label=window.prompt('Seller-facing attribute label:',a.label);if(!label)return;const description=window.prompt('Attribute description:',a.description||'');if(description===null)return;const required=window.confirm('Require this field when a listing is submitted?');const displayOrder=Number(window.prompt('Display order:',String(a.displayOrder)));if(!Number.isFinite(displayOrder))return;const request={expectedVersion:a.version,label,description:description||null,required,searchable:a.searchable,filterable:a.filterable,displayOrder,validation:this.parseJson(a.validationJson),reason:'Reviewed category attribute update'};this.pendingMutation=()=>this.service.updateAttribute(this.id,a.id,request);this.service.attributePreview(this.id,a.id,request).subscribe(this.previewObserver());}
 toggleAttribute(_d:CatalogDetail,id:string,version:number,status:string){const request={expectedVersion:version,status:status==='ACTIVE'?'DISABLED':'ACTIVE',reason:'Reviewed attribute lifecycle update'};this.pendingMutation=()=>this.service.attributeStatus(this.id,id,request);this.service.attributeStatusPreview(this.id,id,request).subscribe(this.previewObserver());}
 addOption(_d:CatalogDetail,attributeId:string){const value=window.prompt('Stable option value (lowercase letters, numbers, hyphens):');if(!value)return;const label=window.prompt('Seller-facing option label:',value);if(!label)return;this.act(this.service.createOption(this.id,attributeId,{value,label,displayOrder:0,reason:'Reviewed enum option addition'}));}
 previewOptionStatus(_d:CatalogDetail,attributeId:string,option:CatalogOption){const request={expectedVersion:option.version,status:option.status==='ACTIVE'?'DISABLED':'ACTIVE',reason:'Reviewed enum option lifecycle update'};this.pendingMutation=()=>this.service.optionStatus(this.id,attributeId,option.id,request);this.service.optionStatusPreview(this.id,attributeId,option.id,request).subscribe(this.previewObserver());}
 createGuidance(d:CatalogDetail){this.act(this.service.createGuidance(this.id,{guidanceType:this.guidanceType,title:this.guidanceTitle.trim(),body:this.guidanceBody.trim(),displayOrder:d.sellerGuidance.length,reason:this.guidanceReason}),()=>{this.guidanceTitle='';this.guidanceBody='';});}
 editGuidance(g:SellerGuidance){const title=window.prompt('Guidance title:',g.title);if(!title)return;const body=window.prompt('Non-blocking seller guidance:',g.body);if(!body)return;this.act(this.service.updateGuidance(this.id,g.id,{expectedVersion:g.version,guidanceType:g.guidanceType,title,body,displayOrder:g.displayOrder,reason:'Reviewed seller guidance update'}));}
 toggleGuidance(_d:CatalogDetail,id:string,version:number,status:string){this.act(this.service.guidanceStatus(this.id,id,{expectedVersion:version,status:status==='ACTIVE'?'DISABLED':'ACTIVE',reason:'Reviewed seller guidance lifecycle update'}));}
 private previewObserver(){return{next:(v:CatalogImpact)=>this.preview.set(v),error:(e:any)=>this.handleError(e)};}
 private parseJson(value:string|null){try{return value?JSON.parse(value):{};}catch{return{};}}
 private act(request:Observable<CatalogDetail>,done?:()=>void){this.saving.set(true);this.error.set('');request.subscribe({next:v=>{this.saving.set(false);this.detail.set(v);this.hydrate(v);done?.();},error:e=>{this.saving.set(false);this.handleError(e);}});}
 private actGoverned(request:Observable<CatalogDetail|CatalogApproval>){this.saving.set(true);this.error.set('');request.subscribe({next:v=>{this.saving.set(false);this.cancelPreview();if('approvalRequired'in v){this.approval.set(v);}else{this.approval.set(null);this.detail.set(v);this.hydrate(v);}},error:e=>{this.saving.set(false);this.handleError(e);}});}
 private handleError(e:any){if(e?.status===409){this.cancelPreview();this.load('Catalog configuration changed while you were working. The latest version has been reloaded.');}else this.error.set(e?.error?.error?.message||'Catalog action could not be completed.');}
}
