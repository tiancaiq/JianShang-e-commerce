import {DatePipe,KeyValuePipe} from '@angular/common';
import {Component,DestroyRef,computed,inject,signal} from '@angular/core';
import {FormsModule} from '@angular/forms';
import {ActivatedRoute,RouterLink,RouterLinkActive} from '@angular/router';
import {HttpErrorResponse} from '@angular/common/http';
import {finalize,Observable} from 'rxjs';
import {takeUntilDestroyed} from '@angular/core/rxjs-interop';
import {AdminService} from '../../core/services/admin.service';
import {AdminGovernanceService} from '../../core/services/admin-governance.service';
import {AdminGovernanceDetail,AdminGovernanceSummary,ApprovalDetail,ApprovalSummary,GovernanceDashboard,GovernancePage,GrantRoleRequest,RevokeRoleRequest,RoleAssignment,RoleChangePreview,RoleDefinition} from '../../core/models/admin-governance.model';
import {governanceCapability} from './governance-capability';

type Section='dashboard'|'admins'|'admin'|'roles'|'approvals'|'approval';

@Component({
 selector:'app-admin-governance',standalone:true,
 imports:[FormsModule,RouterLink,RouterLinkActive,DatePipe,KeyValuePipe],
 template:`<section class="gov-page">
  <header class="hero"><div><p class="eyebrow">Administrator control plane</p><h2>Governance</h2><p>Time-aware admin authority and independent approval for selected high-risk actions.</p></div><span class="mode">{{capability().canManageRoles?'Governed changes':'Read only'}}</span></header>
  <nav class="tabs" aria-label="Governance sections"><a routerLink="/admin/governance" [routerLinkActiveOptions]="{exact:true}" routerLinkActive="active">Overview</a><a routerLink="/admin/governance/admins" routerLinkActive="active">Admins</a><a routerLink="/admin/governance/roles" routerLinkActive="active">Roles</a><a routerLink="/admin/governance/approvals" routerLinkActive="active">Approvals</a></nav>
  @if(error()){<p class="notice error" role="alert">{{error()}}</p>}@if(message()){<p class="notice" role="status">{{message()}}</p>}@if(loading()){<p class="loading">Loading governance evidence…</p>}

  @if(!loading()&&section==='dashboard'&&dashboard();as d){<div class="metrics"><a routerLink="/admin/governance/admins"><span>Admins</span><strong>{{d.admins}}</strong></a><a routerLink="/admin/governance/approvals" [queryParams]="{status:'PENDING'}"><span>Pending approvals</span><strong>{{d.pendingApprovals}}</strong></a><a routerLink="/admin/governance/admins" [queryParams]="{hasTemporaryElevation:true}"><span>Temporary elevations</span><strong>{{d.temporaryElevations}}</strong></a></div><section class="panel"><h3>Recent governance actions</h3><div class="table"><table><thead><tr><th>When</th><th>Action</th><th>Target</th><th>Outcome</th><th>Correlation</th></tr></thead><tbody>@for(e of d.recentActions;track e.eventId){<tr><td>{{e.createdAt|date:'medium'}}</td><td>{{label(e.eventType)}}</td><td>{{e.targetType}}<code>{{e.targetId}}</code></td><td><span class="status" [attr.data-status]="e.outcome">{{e.outcome}}</span></td><td><code>{{e.correlationId}}</code></td></tr>}@empty{<tr><td colspan="5">No governance actions recorded.</td></tr>}</tbody></table></div></section>}

  @if(!loading()&&section==='admins'&&admins();as p){<section class="panel"><div class="section-title"><div><p class="eyebrow">Effective authority</p><h3>Administrators</h3></div><form class="filters" (ngSubmit)="loadAdmins()"><label>Admin search<input name="q" [(ngModel)]="adminQ" placeholder="Name or admin ID"></label><label>Admin role<select name="role" [(ngModel)]="adminRole"><option value="">All roles</option>@for(role of roles();track role.role){<option [value]="role.role">{{label(role.role)}}</option>}</select></label><label>Admin state<select name="state" [(ngModel)]="adminState"><option value="">All states</option><option>ADMIN_ENABLED</option><option>ADMIN_DISABLED</option></select></label><label>Temporary elevation<select name="temporaryElevation" [(ngModel)]="adminTemporaryElevation"><option value="">All elevation states</option><option value="true">Has temporary elevation</option><option value="false">No temporary elevation</option></select></label><button>Filter</button></form></div><div class="table"><table><thead><tr><th>Admin</th><th>Effective roles</th><th>Elevation</th><th>State</th><th>Last change</th></tr></thead><tbody>@for(a of p.items;track a.adminUserId){<tr><td><a [routerLink]="['/admin/governance/admins',a.adminUserId]">{{a.safeDisplayName}}</a><code>{{a.adminUserId}}</code></td><td>{{a.effectiveRoles.join(', ')||'None'}}</td><td>{{a.temporaryElevation?'Temporary':'—'}}</td><td><span class="status" [attr.data-status]="a.adminState">{{label(a.adminState)}}</span></td><td>{{a.lastGovernanceChange?(a.lastGovernanceChange|date:'medium'):'—'}}</td></tr>}@empty{<tr><td colspan="5">No administrators match these filters.</td></tr>}</tbody></table></div></section>}

  @if(!loading()&&section==='admin'&&adminDetail();as a){<a class="back" routerLink="/admin/governance/admins">← Administrators</a><section class="identity"><div><p class="eyebrow">Admin identity</p><h3>{{a.safeDisplayName}}</h3><code>{{a.adminUserId}}</code></div><div><span>{{label(a.marketplaceAccountState)}}</span><strong>{{label(a.adminState)}}</strong></div></section><div class="two-col"><section class="panel"><h3>Effective roles</h3><div class="chips">@for(role of a.effectiveRoles;track role){<span>{{role}}</span>}@empty{<span>None</span>}</div><h4>Effective permissions</h4><div class="permission-list">@for(permission of a.effectivePermissions;track permission){<code>{{permission}}</code>}</div></section><section class="panel"><h3>Role action</h3>@if(a.availableCapabilities.readOnly){<p class="notice">{{a.availableCapabilities.readOnlyReason}}</p>}@else{<label>Role<select [(ngModel)]="grantRole"><option value="">Select role</option>@for(role of roles();track role.role){@if(role.assignable){<option [value]="role.role">{{role.role}}</option>}}</select></label><div class="date-row"><label>Effective<input type="datetime-local" [(ngModel)]="effectiveAt"></label><label>Expires<input type="datetime-local" [(ngModel)]="expiresAt"></label></div><label>Reason<textarea [(ngModel)]="roleReason" rows="3" maxlength="1000"></textarea></label><button class="primary" [disabled]="!grantRole||!roleReason.trim()" (click)="reviewGrant(a)">Dry run</button>}@if(rolePreview();as preview){<div class="impact"><strong>{{preview.changeType}} {{preview.role}}</strong><p>{{preview.currentRoles.join(', ')||'No roles'}} → {{preview.proposedRoles.join(', ')||'No roles'}}</p>@if(preview.permissionsAdded.length){<p>Added: {{preview.permissionsAdded.join(', ')}}</p>}@if(preview.permissionsRemoved.length){<p>Removed: {{preview.permissionsRemoved.join(', ')}}</p>}@for(w of preview.warnings;track w){<p class="warning">{{w}}</p>}<button class="danger" (click)="confirmRoleChange()">{{preview.approvalRequired?'Create approval request':'Confirm governed change'}}</button></div>}</section></div><section class="panel"><h3>Role assignments</h3><div class="table"><table><thead><tr><th>Role</th><th>Window</th><th>Status</th><th>Reason</th><th></th></tr></thead><tbody>@for(x of a.activeAssignments;track x.assignmentId){<tr><td>{{x.role}}<code>{{x.assignmentId}}</code></td><td>{{x.effectiveAt|date:'short'}} → {{x.expiresAt?(x.expiresAt|date:'short'):'No expiry'}}</td><td><span class="status" [attr.data-status]="x.status">{{x.status}}</span></td><td>{{x.reason}}</td><td>@if(a.availableCapabilities.canManageRoles){<button class="small" (click)="reviewRevoke(a,x)">Review revoke</button>}</td></tr>}@for(x of a.historicalAssignments;track x.assignmentId){<tr class="muted"><td>{{x.role}}<code>{{x.assignmentId}}</code></td><td>{{x.effectiveAt|date:'short'}} → {{x.expiresAt?(x.expiresAt|date:'short'):'No expiry'}}</td><td>{{x.status}}</td><td>{{x.revocationReason||x.reason}}</td><td></td></tr>}</tbody></table></div></section><section class="panel"><h3>Governance timeline</h3>@for(e of a.governanceTimeline;track e.eventId){<article class="event"><time>{{e.createdAt|date:'medium'}}</time><strong>{{label(e.eventType)}}</strong><span>{{e.reason}}</span><code>{{e.correlationId}}</code></article>}@empty{<p>No governance events recorded.</p>}</section>}

  @if(!loading()&&section==='roles'){<section class="panel"><div class="section-title"><div><p class="eyebrow">Read-only catalog</p><h3>Roles and permissions</h3></div></div><div class="role-grid">@for(role of roles();track role.role){<article><div><h4>{{role.role}}</h4><span>{{role.assignable?'Assignable':'Reserved'}}</span></div><p>{{role.description}}</p><details><summary>{{role.permissions.length}} permissions</summary>@for(permission of role.permissions;track permission){<code>{{permission}}</code>}</details></article>}</div></section>}

  @if(!loading()&&section==='approvals'&&approvals();as p){<section class="panel"><div class="section-title"><div><p class="eyebrow">Separation of duties</p><h3>Approval inbox</h3></div><form class="filters" (ngSubmit)="loadApprovals()"><label>Approval status<select name="approvalStatus" [(ngModel)]="approvalStatus"><option value="">All statuses</option>@for(status of approvalStatuses;track status){<option [value]="status">{{status}}</option>}</select></label><label>Approval risk<select name="risk" [(ngModel)]="approvalRisk"><option value="">All risks</option><option>HIGH</option><option>CRITICAL</option></select></label><button>Filter</button></form></div><div class="table"><table><thead><tr><th>Action</th><th>Risk</th><th>Target</th><th>Requested by</th><th>Approvals</th><th>Status</th><th>Expires</th></tr></thead><tbody>@for(a of p.items;track a.approvalId){<tr><td><a [routerLink]="['/admin/governance/approvals',a.approvalId]">{{label(a.actionType)}}</a><code>{{a.approvalId}}</code></td><td>{{a.riskLevel}}</td><td>{{a.safeTargetLabel}}<code>{{a.targetId}}</code></td><td><code>{{a.requesterAdminId}}</code></td><td>{{a.currentApprovals}} / {{a.requiredApprovals}}</td><td><span class="status" [attr.data-status]="a.status">{{a.status}}</span></td><td>{{a.expiresAt|date:'short'}}</td></tr>}@empty{<tr><td colspan="7">No approval requests match these filters.</td></tr>}</tbody></table></div></section>}

  @if(!loading()&&section==='approval'&&approvalDetail();as d){<a class="back" routerLink="/admin/governance/approvals">← Approval inbox</a><section class="identity"><div><p class="eyebrow">{{d.approval.riskLevel}} · {{label(d.approval.actionType)}}</p><h3>{{d.safeActionSummary}}</h3><code>{{d.approval.approvalId}}</code></div><div><span>{{d.approval.currentApprovals}} / {{d.approval.requiredApprovals}} approvals</span><strong>{{d.approval.status}}</strong></div></section><div class="two-col"><section class="panel"><h3>Action and policy</h3><dl><div><dt>Requester</dt><dd><code>{{d.approval.requesterAdminId}}</code></dd></div><div><dt>Target</dt><dd>{{d.approval.targetType}} · <code>{{d.approval.targetId}}</code></dd></div><div><dt>Mode</dt><dd>{{label(d.policy.approvalMode)}}</dd></div><div><dt>Expires</dt><dd>{{d.approval.expiresAt|date:'medium'}}</dd></div></dl><h4>Safe action payload</h4>@for(item of d.safeActionPayload|keyvalue;track item.key){<div class="payload"><span>{{item.key}}</span><code>{{item.value}}</code></div>}</section><section class="panel"><h3>Review and execution</h3>@if(d.availableCapabilities.readOnlyReason){<p class="notice">{{d.availableCapabilities.readOnlyReason}}</p>}<label>Decision reason<textarea [(ngModel)]="decisionReason" rows="4" maxlength="1000"></textarea></label><div class="actions"><button [disabled]="!d.availableCapabilities.canApprove||!decisionReason.trim()" (click)="decide('approve')">Approve</button><button [disabled]="!d.availableCapabilities.canReject||!decisionReason.trim()" (click)="decide('reject')">Reject</button><button [disabled]="!d.availableCapabilities.canCancel||!decisionReason.trim()" (click)="cancelApproval()">Cancel</button><button class="primary" [disabled]="!d.availableCapabilities.canExecute" (click)="executeApproval()">Execute approved action</button></div>@if(d.executionReference){<p class="notice">Execution reference: {{d.executionReference}}</p>}@if(d.executionFailureSummary){<p class="notice error">{{d.executionFailureCode}} · {{d.executionFailureSummary}}</p>}</section></div><section class="panel"><h3>Independent decisions</h3>@for(x of d.decisions;track x.decisionId){<article class="event"><time>{{x.createdAt|date:'medium'}}</time><strong>{{x.decision}}</strong><code>{{x.approverAdminId}}</code><span>{{x.reason}}</span></article>}@empty{<p>No decisions recorded.</p>}</section><section class="panel"><h3>Approval timeline</h3>@for(e of d.timeline;track e.eventId){<article class="event"><time>{{e.createdAt|date:'medium'}}</time><strong>{{label(e.eventType)}}</strong><span>{{e.reason}}</span><code>{{e.correlationId}}</code></article>}</section>}
 </section>`,
 styles:[`:host{display:block}.gov-page{display:grid;gap:1.15rem;color:var(--color-text-primary)}.hero{display:flex;justify-content:space-between;gap:2rem;align-items:end;padding:1.5rem;border:1px solid var(--color-border);background:linear-gradient(120deg,var(--color-bg-secondary),rgba(56,189,248,.1))}.hero h2{font-size:clamp(2rem,5vw,3.5rem);margin:.1rem 0}.hero p{max-width:62ch;color:var(--color-text-secondary)}.eyebrow{margin:0!important;text-transform:uppercase;letter-spacing:.13em;font-size:.7rem;font-weight:850;color:var(--color-info)!important}.mode{padding:.45rem .7rem;border:1px solid var(--color-info);white-space:nowrap}.tabs{display:flex;gap:.2rem;border-bottom:1px solid var(--color-border)}.tabs a{padding:.7rem .85rem;color:var(--color-text-secondary);text-decoration:none;border-bottom:3px solid transparent}.tabs a.active{color:var(--color-info);border-color:var(--color-info)}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:.75rem}.metrics a{display:grid;min-height:120px;padding:1rem;border:1px solid var(--color-border);background:var(--color-bg-secondary);color:inherit;text-decoration:none}.metrics strong{font-size:2.4rem;margin-top:auto}.panel{display:grid;gap:.85rem;padding:1rem;border:1px solid var(--color-border);background:var(--color-bg-secondary)}.panel h3,.panel h4{margin:0}.section-title,.identity,.role-grid article>div{display:flex;justify-content:space-between;gap:1rem;align-items:end}.filters,.actions,.date-row{display:flex;gap:.55rem;flex-wrap:wrap;align-items:end}.table{overflow:auto;border:1px solid var(--color-border)}table{width:100%;border-collapse:collapse;min-width:820px}th,td{padding:.7rem;text-align:left;border-bottom:1px solid var(--color-border);vertical-align:top}th{font-size:.68rem;text-transform:uppercase;letter-spacing:.08em;color:var(--color-text-muted)}td{font-size:.84rem}code{display:block;font-size:.72rem;color:var(--color-text-muted);word-break:break-all}a{color:var(--color-info)}.status,.chips span{display:inline-flex;padding:.2rem .48rem;border-radius:999px;background:rgba(148,163,184,.15);font-size:.7rem;font-weight:800}.status[data-status=ACTIVE],.status[data-status=APPROVED],.status[data-status=EXECUTED],.status[data-status=ADMIN_ENABLED]{color:var(--color-success)}.status[data-status=PENDING],.status[data-status=SCHEDULED]{color:#d99a35}.status[data-status=REJECTED],.status[data-status=FAILED],.status[data-status=INVALIDATED],.status[data-status=ADMIN_DISABLED]{color:var(--color-danger)}.identity{padding:1rem;border-left:5px solid var(--color-info);background:var(--color-bg-secondary)}.identity h3{font-size:1.7rem;margin:.2rem 0}.identity>div:last-child{display:grid;text-align:right}.two-col{display:grid;grid-template-columns:1fr 1fr;gap:1rem}.chips,.permission-list{display:flex;gap:.4rem;flex-wrap:wrap}.permission-list code{padding:.3rem;background:var(--color-bg-primary)}label{display:grid;gap:.3rem;font-size:.78rem;font-weight:750}input,select,textarea,button{padding:.6rem;border:1px solid var(--color-border);background:var(--color-bg-primary);color:var(--color-text-primary);font:inherit}button{font-weight:800;cursor:pointer}.primary{background:var(--color-info);color:#05131a}.danger{border-color:var(--color-danger)}button:disabled{opacity:.45;cursor:not-allowed}.impact{display:grid;gap:.5rem;padding:.8rem;border:1px solid var(--color-info)}.impact p{margin:0}.warning{color:#d99a35}.role-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.75rem}.role-grid article{padding:1rem;border:1px solid var(--color-border)}.role-grid code{margin:.25rem 0}.event{display:grid;grid-template-columns:10rem 1fr 2fr;gap:.6rem;padding:.65rem 0;border-bottom:1px solid var(--color-border)}.event code{grid-column:2/-1}.notice,.loading{padding:.8rem 1rem;border-left:4px solid var(--color-info);background:rgba(56,189,248,.08)}.notice.error{border-color:var(--color-danger)}.payload,dl div{display:flex;justify-content:space-between;gap:1rem;padding:.4rem 0;border-bottom:1px solid var(--color-border)}dt{color:var(--color-text-muted)}dd{margin:0;text-align:right}.muted{opacity:.67}.back{width:max-content}@media(max-width:850px){.hero,.section-title,.identity{align-items:start;flex-direction:column}.metrics,.two-col,.role-grid{grid-template-columns:1fr}.event{grid-template-columns:1fr}.event code{grid-column:auto}.identity>div:last-child{text-align:left}}`]
})
export class AdminGovernanceComponent{
 private readonly api=inject(AdminGovernanceService);private readonly admin=inject(AdminService);private readonly route=inject(ActivatedRoute);private readonly destroy=inject(DestroyRef);
 readonly section=(this.route.snapshot.data['governanceSection']??'dashboard') as Section;readonly capability=computed(()=>governanceCapability(this.admin.currentAdmin()?.permissions??[]));readonly loading=signal(false);readonly error=signal('');readonly message=signal('');readonly dashboard=signal<GovernanceDashboard|null>(null);readonly admins=signal<GovernancePage<AdminGovernanceSummary>|null>(null);readonly adminDetail=signal<AdminGovernanceDetail|null>(null);readonly roles=signal<RoleDefinition[]>([]);readonly approvals=signal<GovernancePage<ApprovalSummary>|null>(null);readonly approvalDetail=signal<ApprovalDetail|null>(null);readonly rolePreview=signal<RoleChangePreview|null>(null);
 readonly approvalStatuses=['PENDING','APPROVED','REJECTED','EXECUTED','FAILED','EXPIRED','CANCELLED','INVALIDATED'];
 adminQ='';adminRole='';adminState='';adminTemporaryElevation='';approvalStatus='PENDING';approvalRisk='';grantRole='';effectiveAt='';expiresAt='';roleReason='';decisionReason='';private pendingRoleAction:(()=>Observable<unknown>)|null=null;
 constructor(){
  if(this.section==='admins'){this.route.queryParamMap.pipe(takeUntilDestroyed(this.destroy)).subscribe(params=>{const temporary=params.get('hasTemporaryElevation');this.adminTemporaryElevation=temporary==='true'||temporary==='false'?temporary:'';this.load();});return;}
  if(this.section==='approvals'){this.route.queryParamMap.pipe(takeUntilDestroyed(this.destroy)).subscribe(params=>{const status=params.get('status');this.approvalStatus=status&&this.approvalStatuses.includes(status)?status:'PENDING';this.load();});return;}
  this.load();
 }
 load(){this.loading.set(true);this.error.set('');this.message.set('');let request:Observable<unknown>;switch(this.section){case'dashboard':request=this.api.dashboard();break;case'admins':request=this.api.admins({q:this.adminQ,role:this.adminRole,status:this.adminState,hasTemporaryElevation:this.adminTemporaryElevation,page:0,size:50});break;case'admin':request=this.api.admin(this.route.snapshot.paramMap.get('adminId')||'');break;case'roles':request=this.api.roles();break;case'approvals':request=this.api.approvals({status:this.approvalStatus,riskLevel:this.approvalRisk,page:0,size:50});break;case'approval':request=this.api.approval(this.route.snapshot.paramMap.get('approvalId')||'');break;}request.pipe(finalize(()=>this.loading.set(false)),takeUntilDestroyed(this.destroy)).subscribe({next:v=>this.accept(v),error:e=>this.error.set(this.errorMessage(e))});if((this.section==='admins'||this.section==='admin')&&!this.roles().length)this.api.roles().pipe(takeUntilDestroyed(this.destroy)).subscribe({next:v=>this.roles.set(v)});}
 loadAdmins(){this.section==='admins'&&this.load();}loadApprovals(){this.section==='approvals'&&this.load();}
 private accept(value:unknown){switch(this.section){case'dashboard':this.dashboard.set(value as GovernanceDashboard);break;case'admins':this.admins.set(value as GovernancePage<AdminGovernanceSummary>);break;case'admin':this.adminDetail.set(value as AdminGovernanceDetail);break;case'roles':this.roles.set(value as RoleDefinition[]);break;case'approvals':this.approvals.set(value as GovernancePage<ApprovalSummary>);break;case'approval':this.approvalDetail.set(value as ApprovalDetail);break;}}
 reviewGrant(a:AdminGovernanceDetail){const key=crypto.randomUUID();const body:GrantRoleRequest={role:this.grantRole,effectiveAt:this.iso(this.effectiveAt),expiresAt:this.iso(this.expiresAt),reason:this.roleReason,expectedAdminGovernanceVersion:a.governanceVersion,idempotencyKey:key};this.error.set('');this.api.previewGrant(a.adminUserId,body).subscribe({next:p=>{this.rolePreview.set(p);this.pendingRoleAction=()=>this.api.grant(a.adminUserId,body);},error:e=>this.error.set(this.errorMessage(e))});}
 reviewRevoke(a:AdminGovernanceDetail,x:RoleAssignment){if(!this.roleReason.trim()){this.error.set('Enter the revocation reason in Role action first.');return;}const body:RevokeRoleRequest={expectedAssignmentVersion:x.version,reason:this.roleReason,idempotencyKey:crypto.randomUUID()};this.api.previewRevoke(a.adminUserId,x.assignmentId,body).subscribe({next:p=>{this.rolePreview.set(p);this.pendingRoleAction=()=>this.api.revoke(a.adminUserId,x.assignmentId,body);},error:e=>this.error.set(this.errorMessage(e))});}
 confirmRoleChange(){if(!this.pendingRoleAction)return;this.loading.set(true);this.pendingRoleAction().pipe(finalize(()=>this.loading.set(false))).subscribe({next:(r:any)=>{this.message.set(r.approval?.approvalId?`Approval ${r.approval.approvalId} created.`:'Role assignment updated.');this.rolePreview.set(null);this.pendingRoleAction=null;this.reloadAdmin();},error:e=>this.error.set(this.errorMessage(e))});}
 decide(value:'approve'|'reject'){const d=this.approvalDetail();if(!d)return;this.command(this.api.decide(d.approval.approvalId,value,d.approval.version,this.decisionReason));}cancelApproval(){const d=this.approvalDetail();if(d)this.command(this.api.cancel(d.approval.approvalId,d.approval.version,this.decisionReason));}executeApproval(){const d=this.approvalDetail();if(d)this.command(this.api.execute(d.approval.approvalId,d.approval.version));}
 private command(request:Observable<ApprovalDetail>){this.loading.set(true);this.error.set('');request.pipe(finalize(()=>this.loading.set(false))).subscribe({next:v=>{this.approvalDetail.set(v);this.decisionReason='';this.message.set('Approval state updated.');},error:e=>this.error.set(this.errorMessage(e))});}
 private reloadAdmin(){const id=this.route.snapshot.paramMap.get('adminId');if(id)this.api.admin(id).subscribe({next:v=>this.adminDetail.set(v),error:e=>this.error.set(this.errorMessage(e))});}
 private iso(value:string){return value?new Date(value).toISOString():null;}label(value:string){return(value||'').toLowerCase().replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());}private errorMessage(error:unknown){if(error instanceof HttpErrorResponse)return error.error?.error?.message||error.error?.message||`Request failed (${error.status}).`;return error instanceof Error?error.message:'Governance request failed.';}
}
