# Development Environment Configuration
# Use this file for local development: docker compose --env-file .env.dev up

# Database Configuration
MONGO_DB_USERNAME=root
MONGO_DB_PASSWORD=password
MONGO_DB_NAME=product-service
MONGO_PORT=27017

MYSQL_ROOT_PASSWORD=mysql
MYSQL_PORT=3306

# Business verification local webhook signing
BUSINESS_VERIFICATION_WEBHOOK_SECRET=local-dev-business-webhook-secret

# Keycloak DB (Postgres)
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=password
POSTGRES_DB=keycloak
KEYCLOAK_PORT=8181
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Kafka Configuration
KAFKA_PORT=9092
SCHEMA_REGISTRY_PORT=8081
KAFKA_UI_PORT=8080

# Mail Configuration
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
